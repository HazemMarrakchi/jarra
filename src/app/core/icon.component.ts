/**
 * Iconographie de Jarra.
 *
 * Le système de référence s'appuie sur la police d'icônes Material Symbols Outlined,
 * chargée dans `index.html`. Les icônes sont donc écrites directement dans les gabarits
 * sous forme de ligatures :
 *
 *     <span class="ms ms-18">storefront</span>
 *
 * Aucun composant n'est nécessaire : la ligature est rendue par la police, et la feuille
 * `styles.css` porte les tailles (`.ms-18`, `.ms-24`, `.ms-40`) ainsi que la variante
 * pleine (`.ms-fill`). Cette approche garantit un jeu d'icônes unique, cohérent et
 * aligné sur le système de design fourni.
 */
export {};
